import { expect, test } from "@playwright/test";
const route = "/problems/semya-i-deti/otmena-ogranicheniya-roditelskih-prav/";
test("restriction cancellation landing and canonical scenario", async ({ page }) => { await page.goto(route); await expect(page.getByRole("heading", { level: 1, name: "Отмена ограничения родительских прав" })).toHaveCount(1); await expect(page.getByRole("link", { name: /Отменить ограничение родительских прав/ }).first()).toBeVisible(); await page.goto(`${route}?scenario=cancellation-return`); await expect(page.locator('meta[name="robots"]')).toHaveAttribute("content", /noindex/); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${route.replaceAll("/", "\\/")}$`)); });
test("restriction cancellation starts with the current decision check", async ({ page }) => { await page.goto("/documents/isk-ob-otmene-ogranicheniya-roditelskih-prav/"); await expect(page.getByRole("heading", { level: 1, name: "Иск об отмене ограничения родительских прав" })).toHaveCount(1); await expect(page.getByLabel(/Есть вступившее в законную силу решение/)).toBeVisible(); await expect(page.getByRole("button", { name: "Продолжить" })).toBeDisabled(); });
test("restriction cancellation has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 800 }); await page.goto(route); expect(await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth)).toBe(false); });
