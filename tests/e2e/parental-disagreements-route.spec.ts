import { expect, test } from "@playwright/test";
const route = "/problems/semya-i-deti/raznoglasiya-roditeley-po-vospitaniyu-i-obrazovaniyu/";
test("parental disagreements landing and canonical scenario", async ({ page }) => { await page.goto(route); await expect(page.getByRole("heading", { level: 1, name: "Разногласия родителей по воспитанию и образованию" })).toHaveCount(1); await expect(page.getByRole("link", { name: /Зафиксировать совместное решение/ }).first()).toBeVisible(); await page.goto(`${route}?scenario=guardianship`); await expect(page.locator('meta[name="robots"]')).toHaveAttribute("content", /noindex/); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${route.replaceAll("/", "\\/")}$`)); });
test("parental disagreements starts by choosing the subject", async ({ page }) => { await page.goto("/documents/sovmestnoe-reshenie-roditeley-po-vospitaniyu-i-obrazovaniyu/"); await expect(page.getByRole("heading", { level: 1, name: "Совместное решение родителей по воспитанию и образованию" })).toHaveCount(1); await expect(page.getByLabel("О чём разногласие родителей?")).toBeVisible(); await expect(page.getByRole("button", { name: "Продолжить" })).toBeDisabled(); });
test("parental disagreements has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 800 }); await page.goto(route); expect(await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth)).toBe(false); });
