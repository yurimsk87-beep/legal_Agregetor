import { expect, test } from "@playwright/test";
const route = "/problems/semya-i-deti/vosstanovlenie-v-roditelskih-pravah/";
test("restoration landing and canonical scenario", async ({ page }) => { await page.goto(route); await expect(page.getByRole("heading", { level: 1, name: "Восстановление в родительских правах" })).toHaveCount(1); await expect(page.getByRole("link", { name: /Восстановить родительские права/ }).first()).toBeVisible(); await page.goto(`${route}?scenario=restoration-return`); await expect(page.locator('meta[name="robots"]')).toHaveAttribute("content", /noindex/); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${route.replaceAll("/", "\\/")}$`)); });
test("restoration document starts with the court decision check", async ({ page }) => { await page.goto("/documents/isk-o-vosstanovlenii-v-roditelskih-pravah/"); await expect(page.getByRole("heading", { level: 1, name: "Иск о восстановлении в родительских правах" })).toHaveCount(1); await expect(page.getByLabel(/Есть вступившее в законную силу решение/)).toBeVisible(); await expect(page.getByRole("button", { name: "Продолжить" })).toBeDisabled(); });
test("restoration route has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 800 }); await page.goto(route); expect(await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth)).toBe(false); });
