import { expect, test } from "@playwright/test";

const route = "/problems/semya-i-deti/vyezd-rebenka-za-granitsu/";
test("child travel landing and canonical scenario", async ({ page }) => { await page.goto(route); await expect(page.getByRole("heading", { level: 1, name: "Выезд ребёнка за границу" })).toHaveCount(1); await expect(page.getByRole("link", { name: /Ребёнок едет с одним родителем/ })).toBeVisible(); await page.goto(`${route}?scenario=without-parents`); await expect(page.locator('meta[name="robots"]')).toHaveAttribute("content", /noindex/); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${route.replaceAll("/", "\\/")}$`)); });
test("child travel document starts with scenario-specific question", async ({ page }) => { await page.goto("/documents/vyezd-rebenka-s-odnim-roditelem/"); await expect(page.getByRole("heading", { level: 1, name: "Выезд ребёнка с одним родителем" })).toHaveCount(1); await expect(page.getByLabel("Сведения о сопровождающем законном представителе")).toBeVisible(); await expect(page.getByRole("button", { name: "Продолжить" })).toBeDisabled(); });
test("child travel route has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 800 }); await page.goto(route); expect(await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth)).toBe(false); });
