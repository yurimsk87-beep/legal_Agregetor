import { expect, test } from "@playwright/test";
const path = "/problems/semya-i-deti/dopolnitelnye-rashody-na-rebenka/";
test("additional child expenses landing and canonical scenario", async ({ page }) => { await page.goto(path); await expect(page.getByRole("heading", { level: 1, name: "Дополнительные расходы на ребёнка" })).toHaveCount(1); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${path}$`)); });
test("additional child expenses starts with four choices", async ({ page }) => { await page.goto(path); await expect(page.getByRole("link", { name: /Проверить, относятся ли расходы/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Согласовать дополнительные расходы/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Взыскать уже понесённые расходы/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Взыскать необходимые будущие расходы/ })).toBeVisible(); });
test("additional child expenses has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 900 }); await page.goto(path); const overflow = await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth); expect(overflow).toBe(false); });
