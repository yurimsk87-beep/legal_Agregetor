import { expect, test } from "@playwright/test";
const path = "/problems/semya-i-deti/slozhnye-imushchestvennye-spory-suprugov/";
test("complex marital property landing is canonical", async ({ page }) => { await page.goto(path); await expect(page.getByRole("heading", { level: 1, name: "Сложные имущественные споры супругов" })).toHaveCount(1); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${path}$`)); });
test("complex marital property has five paths", async ({ page }) => { await page.goto(path); for (const scenario of ["debts", "mortgage", "business", "third-party", "bankruptcy-security"]) await expect(page.locator(`a[href$="?scenario=${scenario}"]`).first()).toBeVisible(); });
test("complex marital property has no overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 900 }); await page.goto(path); expect(await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth)).toBe(false); });
test("ordinary division exits before collecting court or property data", async ({ page }) => { await page.goto("/documents/slozhnyy-spor-ob-obshchih-dolgah-suprugov/"); await page.getByLabel("Спор ограничивается обычным разделом имущества без долгов, залога, бизнеса, третьих лиц, банкротства и сложных обеспечительных мер?").selectOption("yes"); await page.getByRole("button", { name: "Получить результат" }).click(); await expect(page.getByRole("link", { name: "Перейти к обычному разделу имущества" })).toHaveAttribute("href", "/problems/semya-i-deti/razvod-i-razdel-imushchestva/"); await expect(page.getByText("Готовность к подаче: нет.")).toBeVisible(); });
test("all five documents have a nonfiling helper", async ({ page }) => { for (const slug of ["slozhnyy-spor-ob-obshchih-dolgah-suprugov", "slozhnyy-spor-ob-ipotechnom-imushchestve-suprugov", "slozhnyy-spor-o-biznes-aktivah-suprugov", "slozhnyy-spor-s-pravami-tretih-lits-i-kompensatsiey", "slozhnyy-spor-pri-bankrotstve-i-obespechitelnye-mery"]) { await page.goto(`/documents/${slug}/`); await expect(page.locator("h1")).toHaveCount(1); await expect(page.getByText("Материал не готов к подаче.").first()).toBeVisible(); } });
