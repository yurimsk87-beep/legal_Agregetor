import { expect, test } from "@playwright/test";
const path = "/problems/semya-i-deti/soderzhanie-supruga-i-byvshego-supruga/";
test("spousal support landing and canonical scenario", async ({ page }) => { await page.goto(path); await expect(page.getByRole("heading", { level: 1, name: "Содержание супруга и бывшего супруга" })).toHaveCount(1); await expect(page.locator('link[rel="canonical"]')).toHaveAttribute("href", new RegExp(`${path}$`)); });
test("spousal support starts with four choices", async ({ page }) => { await page.goto(path); await expect(page.getByRole("link", { name: /Проверить право на содержание/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Согласовать содержание добровольно/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Взыскать содержание в браке/ })).toBeVisible(); await expect(page.getByRole("link", { name: /Взыскать содержание после развода/ })).toBeVisible(); });
test("spousal support has no horizontal overflow at 320px", async ({ page }) => { await page.setViewportSize({ width: 320, height: 900 }); await page.goto(path); const overflow = await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth); expect(overflow).toBe(false); });
