import assert from "node:assert/strict";
import { createParentalRightsRestorationDocxBlob } from "../src/lib/parental-rights-restoration-docx";
import { buildParentalRightsRestorationPdfText, createParentalRightsRestorationPdfBlob } from "../src/lib/parental-rights-restoration-pdf";
import { validateParentalRightsRestoration } from "../src/lib/parental-rights-restoration-validator";
async function run() { const decision = validateParentalRightsRestoration("restoration", { deprivationDecision: "yes", decisionDetails: "Решение суда от 01.02.2024", circumstancesChanged: "yes", changeEvidence: "Документы об изменениях", currentRisks: "no", childAge: "under-10", childAdopted: "no", applicantData: "Иванов Иван, адрес", caregiverData: "Иванова Анна, адрес", childData: "Иванов Пётр, 2018 года рождения", courtRegion: "region-moscow", courtName: "Тверской районный суд города Москвы", courtSource: "https://tverskoy.msk.sudrf.ru/", courtConfirmed: "yes" }); assert.equal(decision.filingReady, false); assert.equal(decision.resultKind, "courtDraft"); assert.match(buildParentalRightsRestorationPdfText(decision), /ЧЕРНОВИК — НЕ ГОТОВ К ПОДАЧЕ/); const [pdf, docx] = await Promise.all([createParentalRightsRestorationPdfBlob(decision), createParentalRightsRestorationDocxBlob(decision.draftText)]); assert.equal(pdf.size > 1000, true); assert.equal(docx.size > 1000, true); console.log(`Parental-rights restoration audit passed: PDF ${pdf.size}, DOCX ${docx.size}.`); }
void run();
