name: Legal reference routes CI

on:
  pull_request:
  push:
    branches:
      - main
      - agent/zags-reference-final-clean
      - agent/divorce-property-route-hardening
      - agent/child-guardianship-reference-route
      - agent/parents-child-after-divorce-reference-route
      - agent/family-law-reference-routes-expansion

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: legal_aggregator_ci
          POSTGRES_USER: legal_user
          POSTGRES_PASSWORD: legal_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U legal_user -d legal_aggregator_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://legal_user:legal_password@localhost:5432/legal_aggregator_ci?schema=public
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
      SITE_URL: http://localhost:3000
      LEAD_ATTACHMENT_STORAGE_ROOT: /tmp/private-lead-attachments

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run prisma:generate
      - run: npx prisma db push --skip-generate
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - name: Run AI Navigator quality check
        run: |
          npm run start -- -p 3000 > /tmp/reference-routes-next.log 2>&1 &
          app_pid=$!
          trap 'kill "$app_pid" 2>/dev/null || true' EXIT

          for attempt in {1..60}; do
            if curl --fail --silent http://localhost:3000/ > /dev/null; then
              npm run quality:ai-navigator
              exit $?
            fi
            sleep 1
          done

          cat /tmp/reference-routes-next.log
          exit 1
      - name: Install Chromium for route acceptance
        run: npx playwright install --with-deps chromium
      - name: Run guardianship route acceptance
        env:
          E2E_SUBMIT_REVIEW: "true"
        run: npm run test:e2e:guardianship
      - name: Run parents and child route acceptance
        run: npm run test:e2e:parents-child
      - name: Run child support route acceptance
        run: npm run test:e2e:child-support
      - name: Run parental rights deprivation route acceptance
        run: npm run test:e2e:parental-rights-deprivation
      - name: Run parental rights restriction route acceptance
        run: npm run test:e2e:parental-rights-restriction
      - name: Run paternity establishment route acceptance
        run: npm run test:e2e:paternity-establishment
      - name: Run paternity contest route acceptance
        run: npm run test:e2e:paternity-contest
      - name: Run adoption route acceptance
        run: npm run test:e2e:adoption
      - name: Run child travel route acceptance
        run: npm run test:e2e:child-travel
      - name: Run child name route acceptance
        run: npm run test:e2e:child-name
      - name: Run parental rights restoration route acceptance
        run: npm run test:e2e:parental-rights-restoration
      - name: Run parental rights restriction cancellation route acceptance
        run: npm run test:e2e:parental-rights-restriction-cancellation
      - name: Run parental disagreements route acceptance
        run: npm run test:e2e:parental-disagreements
      - name: Run additional child expenses route acceptance
        run: npm run test:e2e:additional-child-expenses
      - name: Confirm withdrawn review PDF was deleted
        run: test -z "$(find "$LEAD_ATTACHMENT_STORAGE_ROOT" -type f -name '*.pdf' -print -quit 2>/dev/null)"
      - name: Generate and inspect guardianship PDFs
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils
          export GUARDIANSHIP_PDF_AUDIT_DIR="$RUNNER_TEMP/guardianship-pdf-audit"
          npm run audit:guardianship-pdf
          for pdf in "$GUARDIANSHIP_PDF_AUDIT_DIR"/*.pdf; do
            pages="$(pdfinfo "$pdf" | awk '/^Pages:/ { print $2 }')"
            test "${pages:-0}" -gt 0
            pdftotext "$pdf" "${pdf%.pdf}.txt"
            test -s "${pdf%.pdf}.txt"
            grep -q 'Подготовленные сведения' "${pdf%.pdf}.txt"
            grep -q 'Правовая сверка:' "${pdf%.pdf}.txt"
            grep -q 'Страница 1 из' "${pdf%.pdf}.txt"
            for page in $(seq 1 "$pages"); do
              pdftotext -f "$page" -l "$page" "$pdf" - | grep -q '[[:alnum:]]'
            done
            pdftoppm -png -r 120 "$pdf" "${pdf%.pdf}-page"
            test "$(find "$GUARDIANSHIP_PDF_AUDIT_DIR" -maxdepth 1 -name "$(basename "${pdf%.pdf}")-page-*.png" | wc -l)" -eq "$pages"
          done
      - name: Generate and inspect parents and child PDF
        run: npm run audit:parents-child-pdf
      - name: Generate and inspect child support PDF and DOCX
        run: npm run audit:child-support-pdf
      - name: Generate and inspect parental rights deprivation PDF and DOCX
        run: npm run audit:parental-rights-deprivation-pdf
      - name: Generate and inspect parental rights restriction PDF and DOCX
        run: npm run audit:parental-rights-restriction-pdf
      - name: Generate and inspect paternity establishment PDF and DOCX
        run: npm run audit:paternity-establishment-pdf
      - name: Generate and inspect paternity contest PDF and DOCX
        run: npm run audit:paternity-contest-pdf
      - name: Generate and inspect adoption PDF
        run: npm run audit:adoption-pdf
      - name: Generate and inspect child travel PDF
        run: npm run audit:child-travel-pdf
      - name: Generate and inspect child name PDF
        run: npm run audit:child-name-pdf
      - name: Generate and inspect parental rights restoration PDF and DOCX
        run: npm run audit:parental-rights-restoration-pdf
      - name: Generate and inspect parental rights restriction cancellation PDF and DOCX
        run: npm run audit:parental-rights-restriction-cancellation-pdf
      - name: Generate and inspect parental disagreements PDF and DOCX
        run: npm run audit:parental-disagreements-pdf
      - name: Generate and inspect additional child expenses PDF and DOCX
        run: npm run audit:additional-child-expenses-pdf
      - name: Upload PDF and browser acceptance artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: guardianship-acceptance
          path: |
            ${{ runner.temp }}/guardianship-pdf-audit
            playwright-report
            test-results
