import Link from "next/link";
import { PARENTAL_RIGHTS_RESTORATION_SCENARIOS } from "@/data/parental-rights-restoration-route";
export function ParentalRightsRestorationScenarioOverview({ basePath }: { basePath: string }) { return <section className="mt-10 border-t border-line pt-7"><h2 className="text-2xl font-semibold text-ink">Что проверяет каждый путь</h2><div className="mt-5 grid gap-5">{Object.values(PARENTAL_RIGHTS_RESTORATION_SCENARIOS).map((scenario) => <article key={scenario.key} className="border-l-4 border-trust bg-white p-5 shadow-sm"><h3 className="text-xl font-semibold text-ink">{scenario.title}</h3><p className="mt-2 text-sm leading-6 text-zinc-700">{scenario.description[0]}</p><p className="mt-3 text-sm font-medium text-amber-900">{scenario.warning}</p><Link href={`${basePath}?scenario=${scenario.key}`} className="mt-4 inline-flex min-h-11 items-center font-semibold text-trust underline underline-offset-4 focus:outline-none focus:ring-2 focus:ring-trust/30">Открыть сценарий</Link></article>)}</div></section>; }
