export function ensurePaternityEstablishmentDraftMarker(text: string) { const clean = text.trimStart(); return clean.startsWith("ЧЕРНОВИК — НЕ ГОТОВ К ПОДАЧЕ") ? clean : `ЧЕРНОВИК — НЕ ГОТОВ К ПОДАЧЕ\nТРЕБУЕТСЯ ЮРИДИЧЕСКАЯ ПРОВЕРКА\n\n${clean}`; }
export async function createPaternityEstablishmentDocxBlob(text: string) { const { Document, Packer, Paragraph } = await import("docx"); const document = new Document({ sections: [{ properties: {}, children: ensurePaternityEstablishmentDraftMarker(text).split("\n").map((line) => new Paragraph({ text: line })) }] }); return Packer.toBlob(document); }
export function getPaternityEstablishmentDocxFilename(slug: string) { return `CHERNOVIK-${slug}.docx`; }
